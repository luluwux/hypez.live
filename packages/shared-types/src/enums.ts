export enum ServerSortField {
    VOTES = 'votes',
    NEWEST = 'newest',
    OLDEST = 'oldest',
    VOICE = 'voice',
    BOOST = 'boost',
    MEMBERS = 'members',
    RANDOM = 'random',
    CREATED_AT = 'createdAt',
}

export enum PremiumTier {
    NONE = 'NONE',
    PREMIUM = 'PREMIUM',
    TOKEN = 'TOKEN',
}
